<footer>
    <div class="container">
        <div class="row">
            <div class="col-md-4 mb-4">
                <h5><i class="fas fa-industry"></i> Kartal Metal İş</h5>
                <p class="mt-3">Kaliteli metal işçiliği ve kaynak hizmetleri. Müşteri memnuniyeti odaklı çözümler.</p>
                <div class="mt-3">
                    <% if (typeof settings !=='undefined' && settings.social_media) { %>
                        <% if (settings.social_media.facebook && settings.social_media.facebook !=='#' ) { %>
                            <a href="<%= settings.social_media.facebook %>" class="me-3" target="_blank"><i
                                    class="fab fa-facebook fa-2x"></i></a>
                            <% } else { %>
                                <a href="#" class="me-3"><i class="fab fa-facebook fa-2x"></i></a>
                                <% } %>
                                    <% if (settings.social_media.instagram && settings.social_media.instagram !=='#' ) {
                                        %>
                                        <a href="<%= settings.social_media.instagram %>" class="me-3" target="_blank"><i
                                                class="fab fa-instagram fa-2x"></i></a>
                                        <% } else { %>
                                            <a href="#" class="me-3"><i class="fab fa-instagram fa-2x"></i></a>
                                            <% } %>
                                                <% if (settings.social_media.twitter && settings.social_media.twitter
                                                    !=='#' ) { %>
                                                    <a href="<%= settings.social_media.twitter %>" class="me-3"
                                                        target="_blank"><i class="fab fa-twitter fa-2x"></i></a>
                                                    <% } else { %>
                                                        <a href="#" class="me-3"><i
                                                                class="fab fa-twitter fa-2x"></i></a>
                                                        <% } %>
                                                            <% if (settings.social_media.linkedin &&
                                                                settings.social_media.linkedin !=='#' ) { %>
                                                                <a href="<%= settings.social_media.linkedin %>"
                                                                    target="_blank"><i
                                                                        class="fab fa-linkedin fa-2x"></i></a>
                                                                <% } else { %>
                                                                    <a href="#"><i
                                                                            class="fab fa-linkedin fa-2x"></i></a>
                                                                    <% } %>
                                                                        <% } else { %>
                                                                            <a href="#" class="me-3"><i
                                                                                    class="fab fa-facebook fa-2x"></i></a>
                                                                            <a href="#" class="me-3"><i
                                                                                    class="fab fa-instagram fa-2x"></i></a>
                                                                            <a href="#" class="me-3"><i
                                                                                    class="fab fa-twitter fa-2x"></i></a>
                                                                            <a href="#"><i
                                                                                    class="fab fa-linkedin fa-2x"></i></a>
                                                                            <% } %>
                </div>
            </div>

            <div class="col-md-4 mb-4">
                <h5><i class="fas fa-link"></i> Hızlı Linkler</h5>
                <ul class="list-unstyled mt-3">
                    <li class="mb-2"><a href="/"><i class="fas fa-chevron-right"></i> Anasayfa</a></li>
                    <li class="mb-2"><a href="/products"><i class="fas fa-chevron-right"></i> Ürünlerimiz</a></li>
                    <li class="mb-2"><a href="/services"><i class="fas fa-chevron-right"></i> Hizmetlerimiz</a></li>
                    <li class="mb-2"><a href="/about"><i class="fas fa-chevron-right"></i> Hakkımızda</a></li>
                    <li class="mb-2"><a href="/contact"><i class="fas fa-chevron-right"></i> İletişim</a></li>
                </ul>
            </div>

            <div class="col-md-4 mb-4">
                <h5><i class="fas fa-phone"></i> İletişim</h5>
                <ul class="list-unstyled mt-3">
                    <% if (typeof settings !=='undefined' && settings.contact_address) { %>
                        <li class="mb-2">
                            <a href="https://maps.app.goo.gl/v3iJFbEvdHdMFmf2A" target="_blank"
                                class="text-decoration-none text-white">
                                <i class="fas fa-map-marker-alt"></i>
                                <%= settings.contact_address %>
                            </a>
                        </li>
                        <% } else { %>
                            <li class="mb-2"><i class="fas fa-map-marker-alt"></i> Adres bilgisi</li>
                            <% } %>
                                <% if (typeof settings !=='undefined' && settings.contact_phone) { %>
                                    <li class="mb-2">
                                        <a href="tel:<%= settings.contact_phone.replace(/\s/g, '') %>"
                                            class="text-decoration-none text-white">
                                            <i class="fas fa-phone"></i>
                                            <%= settings.contact_phone %>
                                        </a>
                                    </li>
                                    <% } else { %>
                                        <li class="mb-2"><i class="fas fa-phone"></i> +90 XXX XXX XX XX</li>
                                        <% } %>
                                            <% if (typeof settings !=='undefined' && settings.contact_email) { %>
                                                <li class="mb-2">
                                                    <a href="mailto:<%= settings.contact_email %>"
                                                        class="text-decoration-none text-white">
                                                        <i class="fas fa-envelope"></i>
                                                        <%= settings.contact_email %>
                                                    </a>
                                                </li>
                                                <% } else { %>
                                                    <li class="mb-2"><i class="fas fa-envelope"></i>
                                                        <a href="mailto:info@kartalmetal.com"
                                                            class="text-decoration-none text-white">info@kartalmetal.com</a>
                                                    </li>
                                                    <% } %>
                                                        <% if (typeof settings !=='undefined' &&
                                                            settings.working_hours_weekdays) { %>
                                                            <li class="mb-2"><i class="fas fa-clock"></i>
                                                                <%= settings.working_hours_weekdays %>
                                                            </li>
                                                            <% } else { %>
                                                                <li class="mb-2"><i class="fas fa-clock"></i> Pzt-Cmt:
                                                                    08:00 - 18:00</li>
                                                                <% } %>
                </ul>
            </div>
        </div>

        <div class="footer-bottom">
            <p class="mb-0">&copy; <%= new Date().getFullYear() %> Kartal Metal İş. Tüm hakları saklıdır.</p>
        </div>
    </div>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<!-- jQuery -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<!-- Swiper JS -->
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

<script>
    // Image carousel auto-rotate
    class ImageCarousel {
        constructor(containerId, interval = 3000) {
            this.container = document.getElementById(containerId);
            if (!this.container) return;

            this.images = this.container.querySelectorAll('.carousel-image');
            this.dots = this.container.querySelectorAll('.carousel-dot');
            this.currentIndex = 0;
            this.interval = interval;
            this.autoPlayTimer = null;

            this.init();
        }

        init() {
            if (this.images.length <= 1) return;

            this.showImage(0);
            this.startAutoPlay();

            this.dots.forEach((dot, index) => {
                dot.addEventListener('click', () => {
                    this.stopAutoPlay();
                    this.showImage(index);
                    this.startAutoPlay();
                });
            });
        }

        showImage(index) {
            this.images.forEach(img => img.classList.remove('active'));
            this.dots.forEach(dot => dot.classList.remove('active'));

            this.images[index].classList.add('active');
            this.dots[index].classList.add('active');
            this.currentIndex = index;
        }

        next() {
            const nextIndex = (this.currentIndex + 1) % this.images.length;
            this.showImage(nextIndex);
        }

        startAutoPlay() {
            this.autoPlayTimer = setInterval(() => this.next(), this.interval);
        }

        stopAutoPlay() {
            if (this.autoPlayTimer) {
                clearInterval(this.autoPlayTimer);
            }
        }
    }

    // Initialize all carousels on page
    document.addEventListener('DOMContentLoaded', function () {
        document.querySelectorAll('.image-carousel').forEach((carousel, index) => {
            new ImageCarousel(carousel.id || 'carousel-' + index);
        });
    });
</script>

<% if (typeof additionalScripts !=='undefined' ) { %>
    <%- additionalScripts %>
        <% } %>
            </body>

            </html>